

logging:
  level:
    org.apache.activemq.artemis.jms.bridge: TRACE

spring:
  ssl:
    bundle:
      jks:
        mqssl:
          truststore:
            location: "classpath:clientkey.jks"
            password: changeit
