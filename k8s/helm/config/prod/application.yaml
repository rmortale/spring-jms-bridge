logging:
  level:
    org.apache.activemq.artemis.jms.bridge: TRACE

spring:
  ssl:
    bundle:
      jks:
        mqssl:
          truststore:
            location: "file:truststore/clientkey.jks"
            password: changeit
